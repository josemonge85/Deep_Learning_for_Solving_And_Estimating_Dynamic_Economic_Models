# data/

Auto-generated and small fixed datasets used by notebooks.

- `generated/` — outputs of `scripts/` data-generation utilities.

Large or sensitive datasets are *not* committed; notebooks regenerate
them locally where possible.
