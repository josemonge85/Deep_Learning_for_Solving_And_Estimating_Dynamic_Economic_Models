"""Smoke harness skeleton.

Real implementation will iterate over course.yml, execute each
non-`smoke_disabled` notebook in smoke mode, and assert non-error
completion. Currently a no-op.
"""
def test_notebook_execution_placeholder():
    assert True
